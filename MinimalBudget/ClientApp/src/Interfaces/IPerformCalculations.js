/**
 * @param {number} income
 * @param {number} currentBalance
 * @description Takes the current balance and adds income to that balance
 */
function AddIncome(currentBalance, income) {
    return currentBalance + income;
};

function SubtractIncome(currentBalance, purchaseCost) {
    return currentBalance - purchaseCost;
};